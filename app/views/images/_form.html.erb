<div class="form-row form-group-lg">
  <%= f.text_field :url, placeholder: "URL", class: "form-control" %>
</div>
<br>
<div class="actions">
  <%= f.submit class: 'btn btn-lg btn-add' %>
</div>

<br>
<% if !@errors.nil? %>
  <div class="error">
      <label><%= pluralize(@errors.count, "error") %> prohibited this pin from
      being saved:</label>
    <ul>
      <% @errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>  
  </div>  
<% end %>